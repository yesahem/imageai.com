export abstract class BaseModel{
    constructor(){}
    
    private async  generateImage(prompt:string, tensorPath: string, scale: number) {
        
    }
    
    
    private async  trainModel(inputImagesAsZipUrl: string[], triggerWord: string) {
        
    }
}